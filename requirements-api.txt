# Runtime dependencies for the API container.
# Pinned exactly: a transitive minor bump (chromadb in particular) silently
# breaking an image at 2am is not a debugging session worth having.
# Deliberately excludes streamlit — the UI is a separate client, not part of
# the service image.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4

openai==3.3.1
chromadb==1.5.9
langgraph==1.2.11
rank-bm25==0.2.2
python-dotenv==1.2.3
pypdf==6.16.2

boto3==1.42.15
